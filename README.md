Скопировать переменные окружения:
```bash
cp .env.example .env
```

Запустить можно при помощи команды:
```bash
make up
```
или команды на которую указывает команда в Makefile

Миграции и заполнение тестовыми данными выполняется автоматически
